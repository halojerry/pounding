import React from 'react';
import SettingsPageWrapper from '../components/SettingsPageWrapper';

const DisplaySettings: React.FC = () => {
  return (
    <SettingsPageWrapper title='Display'>
      <p>Display settings</p>
    </SettingsPageWrapper>
  );
};

export default DisplaySettings;
